# .tmux

to install plugin manager
```shell
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

to reload
```shell
<leader> r
```

to install plugins
```shell
<leader> <shift> i
```
